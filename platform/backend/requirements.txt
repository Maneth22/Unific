# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Settings & validation
pydantic>=2.8.0
pydantic-settings>=2.4.0
email-validator>=2.2.0

# Database
sqlalchemy[asyncio]>=2.0.32
asyncpg>=0.29.0
alembic>=1.13.2

# Security
argon2-cffi>=23.1.0
pyjwt>=2.9.0
cryptography>=43.0.0
slowapi>=0.1.9

# Utilities
python-dotenv>=1.0.1
httpx>=0.27.0

# Session cache & scheduling (WhatsApp agent Redis sessions, EOD flush)
redis>=5.0.0
apscheduler>=3.10.4

# Background job queue (Phase 0 skeleton — app/worker.py; the EOD-flush
# cron above is migrated onto this in a later phase, see docs/adr/0002)
arq>=0.26.0

# LLM
google-genai>=1.0.0

# Video conferencing
livekit-api>=0.8.0
livekit>=1.1.0

# LiveKit Agents (live captions / dubbed audio / chat translation) — runs
# embedded in this same process, see app/meeting_room/live_agents/. Google
# is the only STT/TTS provider (Deepgram/Azure/OpenAI/ElevenLabs were
# removed — Google-only is a deliberate product decision); silero backs
# shared VAD, unrelated to the provider choice.
livekit-agents[google,silero]>=1.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
